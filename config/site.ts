export const siteConfig = {
  title: "Lingua Time",
  description:
    "A smart datetime picker which understands natural language input",
  url:
    process.env.NODE_ENV === "production"
      ? "https://linguatime.nainglinnkhant.com"
      : "http://localhost:3000",
}
